---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {}

const { class: className, ...props } = Astro.props;
---

<div class={`layout-inner-grid ${className ?? ""}`} {...props}>
    <slot />
</div>
